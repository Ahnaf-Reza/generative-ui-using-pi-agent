GEMINI_API_KEY=YOUR_GEMINI_API_KEY_HERE
PI_PROVIDER=google
PI_MODEL=google.gemma-4-31b-it
PORT=4000
# NOTE: Do NOT store real secrets in this file if it will be committed.
# Copy this to `.env` and fill in real keys locally instead.